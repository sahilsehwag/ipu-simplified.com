(function () {
    var app, noticesController;

    app = angular.module('app');

    noticesController = app.controller('noticesController', function ($scope, $rootScope, $http) {
    });

}).call(this);
