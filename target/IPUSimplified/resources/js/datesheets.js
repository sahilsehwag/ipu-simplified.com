(function () {
    var app, datesheetsController;

    app = angular.module('app');

    datesheetsController = app.controller('datesheetsController', function ($scope, $rootScope, $http) {
    });

}).call(this);
