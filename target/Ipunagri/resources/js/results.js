(function () {
    var app, downloadsController;

    app = angular.module('app');

    downloadsController = app.controller('resultsController', function ($scope, $rootScope, $http) {
    });

}).call(this);
